import { connect } from 'react-redux';
import { } from './actions';
import {{pascalCase name}} from '../../../components/molecules/{{pascalCase name}}';

export const mapStateToProps = state => ({
// todos: state.todos
})

export const mapDispatchToProps = dispatch => {
return {
//inviteAFriend: email => {
// dispatch(inviteAFriend(email));
//},
};
};

export default connect(
mapStateToProps,
mapDispatchToProps
)({{pascalCase name}});